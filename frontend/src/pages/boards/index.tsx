import React from 'react';

const ProjectBoardsPage: React.FC = (...p) => {
  console.log(p);

  //   useProjectBoards();

  return (
    <>
      {/* <Conditional condition={!loading} fallback={<LoadingCards />}>
        <GridWrapper
          data={data}
          Renderer={(d) => <ProjectCard name={''} description={''} {...d} />}
        />
      </Conditional> */}
    </>
  );
};

export default ProjectBoardsPage;
